# Concise Planning Rules

## 1. Task Checklists
- Break tasks down into vertical slices (meaningful increments of end-to-end functionality).
- Use `[ ]` for uncompleted tasks, `[/]` for in-progress tasks, and `[x]` for completed tasks.
